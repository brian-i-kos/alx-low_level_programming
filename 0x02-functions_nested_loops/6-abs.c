#include "main.h"

/**
 * _abs - computes absolute values of an integer
 * @n: is an integer
 *
 * Return : is an integer
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
